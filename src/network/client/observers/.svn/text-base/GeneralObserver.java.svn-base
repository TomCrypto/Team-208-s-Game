package network.client.observers;

/**
 * This observer listens to general events,
 * such as an abrupt disconnection or other
 * error.
 * 
 * @author Thomas Beneteau (300250968)
 */
public interface GeneralObserver
{
	/**
	 * Called  when the  client loses connection
	 * to the game server.
	 *
	 * @param exception  The error  causing the
	 * disconnection (may be {@code null} if no
	 * cause exists or can be identified).
	 */
	public void connectionLost(Exception exception);
}
