package network.server;

/**
 * This is to mark classes which are going to
 * be taking in parts of a Configuration class.
 * They must provide a validation method that
 * checks that the configuration values make
 * sense.
 * 
 * @author Thomas Beneteau (300250968)
 */
public interface Configurable
{
	/**
	 * This  method  will   validate  a  server
	 * configuration and will throw appropriate
	 * exceptions if one  or more configuration
	 * fields are not valid.
	 *
	 * @param config The given configuration.
	 */
	public void validate(Configuration config);
}
