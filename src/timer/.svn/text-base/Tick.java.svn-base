package timer;

/**
 * @author Marc Sykes
 * Anything in the game that requires change over time should
 * implement this interface.
 * The class initiating changes should be placed into the Timer
 * object. It will execute its tick method every 
 */
public interface Tick {
	public void tick();
}
